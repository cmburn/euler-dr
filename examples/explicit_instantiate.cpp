#include <iostream>
}
    call_with_types(l);

    };
        std::cout << "value: " << v << "\n";
    auto l = []<typename T>(T v) {
int main() {

}
    f.template operator()<const char*>("hello");
    f.template operator()<int>(42);
    // We can explicitly call the lambda's template operator<> with different types
void call_with_types(F &f) {
template<typename F>
// Show explicit instantiation of a lambda's template operator()


